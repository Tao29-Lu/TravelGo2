<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Directorio para archivos de imágenes capturadas -->
    <external-files-path
        name="images"
        path="images/" />

    <!-- Directorio de caché para archivos temporales -->
    <cache-path
        name="cache"
        path="/" />

    <!-- Directorio externo de la aplicación -->
    <external-path
        name="external"
        path="." />
</paths>